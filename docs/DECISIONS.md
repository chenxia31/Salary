# DECISIONS · 决策记录

> **只追加，不修改、不删除。** 决策变了就新增一条，并在旧条目下方注明「已被 #N 取代」。
> AI 代理**无权自行拍板**标记为「待决策」的条目 —— 那是人的活。
> 一条决策 ≤ 15 行。写不下说明你在写方案文档，那该进 `specs/`。

格式：

```
## #<编号> <一句话决策> · <日期> · <状态>
- 背景：为什么现在要决定这件事
- 选项：A / B / C，各自代价
- 结论：选了什么
- 代价：接受了什么坏处（必填，没有坏处说明你没想清楚）
- 不可逆性：可逆 / 部分可逆 / 不可逆
```

状态：`待决策` → `已定` → `已被 #N 取代`

---

## #001 采用 AGENTS.md 作为多 AI 唯一指令源 · 2026-09-06 · 已定

- 背景：Claude Code、Gemini CLI 等多 AI 协作时规范易漂移
- 选项：
  - A 各写各的 —— 必然不一致
  - B 符号链接 —— 无法写工具专属规则
  - C AGENTS.md 为唯一真源，各工具文件用 @AGENTS.md 导入
- 结论：选 C
- 代价：每个工具多一层间接文件
- 不可逆性：可逆

---

## #002 采用原生 macOS 14+ / SwiftUI 与 Swift Testing · 2026-09-06 · 已定

- 背景：需在 macOS 状态栏展现 Apple HIG 奢华质感并确保高稳定性与可测性
- 选项：
  - A 引入第三方 Electron / Webview 壳 —— 内存占用高、无法获得原生毛玻璃质感与极佳响应
  - B 纯 AppKit 原生写 —— 代码冗长，维护成本高
  - C 现代 SwiftUI (macOS 14+) + AppKit 桥接状态栏 + Swift Testing 纯逻辑覆盖
- 结论：选 C
- 代价：需要 macOS 14+ 系统环境
- 不可逆性：可逆

---

## #003 纯函数计算引擎 SalaryEngine 与 UI 完全解耦 · 2026-09-06 · 已定

- 背景：金额与流速实时计算涉及跨时区、午休、法定天数(21.75)、周末模式，容错要求极高
- 选项：
  - A 计算散落在 View / Timer 里 —— 无法单测，逻辑易错
  - B 独立纯函数引擎 `SalaryEngine`，输入配置与当前时间直接输出金额、流速、进度
- 结论：选 B
- 代价：增加了一层 Store 驱动层，需保持纯函数数据流
- 不可逆性：可逆

---

## #004 采用静态库与 SPM 双轨架构 · 2026-09-06 · 已定

- 背景：既需要支持 swift test / swift build 极速测试，又需要支持原生 Xcode 工程生成与打包
- 选项：
  - A 仅保留 SPM Package.swift —— Xcode 打开缺少应用 Target 预设配置
  - B 仅保留 xcodebuild —— CLI 环境测试与脚本化不便
  - C Package.swift + project.yml (xcodegen) 双轨，依赖层使用静态库避免 ad-hoc codesign 冲突
- 结论：选 C
- 代价：修改模块结构时需同步更新 Package.swift 与 project.yml
- 不可逆性：可逆

---

## #005 30天免费试用与 ¥1.99 商业化解锁机制 · 2026-09-06 · 已定

- 背景：需要引入低门槛商业化机制（30天免费试用，过期支付 ¥1.99 解锁）
- 选项：
  - A 纯本地时间戳试用 —— 容易被篡改系统时间绕过
  - B 引入第三方收费 SDK (如 RevenueCat) —— 违反 Never 规则（不引入未经评审的第三方依赖）
  - C 原生 StoreKit 2 + 本地安全持久化防篡改 + 内置开发自测开关
- 结论：选 C
- 代价：需要维护本地试用期与 StoreKit 2 交易监听双态
- 不可逆性：可逆

---

## #006 采用自动化脚本生成独立 Release DMG 分发镜像 · 2026-09-06 · 已定

- 背景：用户需要一键安装体验应用，避免依赖本地 Xcode 编译
- 选项：
  - A 仅提供源码让用户自行编译 —— 对非开发人员门槛过高
  - B 引入第三方 DMG 制作工具 (如 create-dmg npm/homebrew) —— 增加外部环境依赖
  - C 编写原生 `scripts/build_dmg.sh` 脚本，利用 macOS 内置 `hdiutil` 生成 UDZO 压缩镜像与 Applications 快捷方式
- 结论：选 C
- 代价：需要维护 shell 打包脚本与 Release 构件生命周期
- 不可逆性：可逆

---

## #007 引入微信/支付宝双通道二维码扫码支付与一键激活机制 · 2026-09-06 · 已定

- 背景：国内用户与独立开发者更习惯通过微信/支付宝扫码完成赞赏与小额付费，免去 App Store 绑定流程
- 选项：
  - A 强制要求用户配置 Apple 开发者沙盒账号通过 StoreKit 购买 —— 国内用户门槛过高，流程繁琐
  - B 接入第三方商业聚合支付 SDK —— 违反 Never 规则（不引入第三方网络/支付 SDK，存在隐私与安全风险）
  - C 原生 CoreImage CIQRCodeGenerator 动态生成微信/支付宝二维码卡片 + 扫码后一键激活 PRO 授权
- 结论：选 C
- 代价：依赖用户自愿付费与诚信扫码（防君子不防小人，适合个人独立开发与开源赞助模式）
- 不可逆性：可逆

---

## #008 将商业强制解锁调整为三档随喜赞助模式 · 2026-09-06 · 已定

- 背景：单一生硬的买断付费容易带来强制商业感，而开源独立开发更适合有温度的“请喝水/加鸡腿/喝咖啡”随喜赞助
- 选项：
  - A 固定单一金额强制付费解锁 —— 商业感重，打工人抵触心理大
  - B 调整为三档赞助（¥1.99喝杯水 / ¥5.99加鸡腿 / ¥9.99瑞幸咖啡），任意赞助均可点亮全部特权
- 结论：选 B
- 代价：需要重新组织赞助卡片 UI 与各处入口文案
- 不可逆性：可逆

---

## #009 确立诚信随喜赞助机制 (Honor System) 与应用内全场景 Logo 贯通 · 2026-09-09 · 已定

- 背景：确定 PRO 会员激活记录方案，权衡外部联网鉴权、卡密体系与本地隐私体验的取舍
- 选项：
  - A 离线对称加密卡密 —— 增加繁琐的输码激活步骤，且本地应用仍可被反编译暴力绕过
  - B 云端订单与 Webhook 鉴权 —— 破坏了纯离线隐私承诺，且产生长期的服务器运维负担与网络延迟
  - C 诚信随喜打赏机制（Honor System / 捐赠软件）—— 100% 本地记录与无痕激活，尊重用户自觉
- 结论：选 C，同时在应用内（看板头部、赞助弹窗、偏好设置与关于页）全场景统一展现高定 App Logo
- 代价：防君子不防小人，但极大降低了用户摩擦，捍卫了离线隐私并实现零运维成本
- 不可逆性：可逆

---

## #010 赞助体系新增「¥0 无须赞助」零门槛直接激活档位 · 2026-09-09 · 已定

- 背景：贯彻「打工人不为难打工人」原则，为暂无预算或只想体验的用户提供体面的免费激活通道，同时保留随喜赞助
- 选项：
  - A 仅提供付费赞助档位（¥1.99起），强制要求扫码
  - B 新增「¥0 无须赞助」专属档位，直接提供一键激活按钮，免去扫码展示与心理负担
- 结论：选 B
- 代价：降低了短期赞助转化率，但赢得了极致的用户信任与开源社区口碑
- 不可逆性：可逆

---

## #011 采用显式 Info.plist 与 Assets.xcassets 双轨机制彻底解决 macOS Finder 应用图标显示 · 2026-09-09 · 已定

- 背景：macOS 14+ / Sonoma / Sequoia Finder 优先读取编译后的 `Assets.car` 与显式 `CFBundleIconFile` / `CFBundleIconName`。此前因 XcodeGen 默认开启 `GENERATE_INFOPLIST_FILE: YES` 导致导出的 Info.plist 缺少图标键，Finder 呈现默认线框网格占位符
- 选项：
  - A 仅在项目配置中传递 build setting —— 经测试 Xcode 编译不会将 `INFOPLIST_KEY_CFBundleIconFile` 注入生成的 plist
  - B 采用标准 macOS 开发规范：创建专用 `App/Info.plist` 显式声明 `CFBundleIconFile: AppIcon`，并生成完整的 `Resources/Assets.xcassets/AppIcon.appiconset` 由 `actool` 编译生成 `Assets.car`
- 结论：选 B，同时在 DMG 构建脚本中配置卷标图标与文件属性刷新
- 代价：项目中增加了 `App/Info.plist` 与 `Assets.xcassets` 结构
- 不可逆性：可逆


